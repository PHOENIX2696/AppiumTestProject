test_env=${test.env}
server_url=${appium.server.url}
device_platform=${device.platform.name}
device_name=${device.name}
device_name_local=${device.name.local}
application_path=${application.path}
os_version=${device.os.version}
device_os_version=${device.os.version}
AVD=${avd.Name}